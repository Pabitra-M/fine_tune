# ── Core ML ───────────────────────────────────────────────────────────────────
torch>=2.1.0
# NOTE: transformers 4.40.x is incompatible with accelerate>=1.x (causes .to()
#       crash on 4-bit models). Pin both to a compatible pair:
transformers>=4.46.0
accelerate>=0.34.0,<2.0.0

# ── QLoRA / PEFT ──────────────────────────────────────────────────────────────
peft>=0.10.0
bitsandbytes>=0.43.0

# ── Data ──────────────────────────────────────────────────────────────────────
datasets>=2.19.0
scikit-learn>=1.3.0
numpy>=1.24.0

# ── Evaluation ────────────────────────────────────────────────────────────────
sacrebleu>=2.3.0

# ── Tokenizer helpers ─────────────────────────────────────────────────────────
sentencepiece>=0.1.99
protobuf>=3.20.0
tokenizers>=0.19.0

# ── Misc utilities (stdlib on most systems, but explicit for HPC envs) ────────
# subprocess, json, math, os, datetime — all stdlib, no install needed